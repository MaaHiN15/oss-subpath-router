services:
  kafka-ui:
    image: provectuslabs/kafka-ui:v0.7.2
    container_name: kafka-ui
    restart: always
    environment:
      - SERVER_SERVLET_CONTEXT_PATH=/kafka
    ports:
      - "8081:8080"
