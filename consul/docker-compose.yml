services:
  consul:
    image: hashicorp/consul:1.22
    container_name: consul
    restart: always
    command:
      - 'agent'
      - '-dev'
      - '-client=0.0.0.0'
      - '-ui-content-path=/consul/'
    ports:
      - "8500:8500"
